---
title: Markdown 样式全览
date: 2026-07-08
brief: 本站支持的全部 Markdown 元素排版效果，自查用。
tags: [博客]
draft: true
---

这篇是排版自查页（draft，不会上线），把 Markdown 和 GFM 支持的元素尽量列全。

## 二级标题

正文段落。中文与 English 混排时的效果，注意拉丁字符用的是 Inter，数字 1234567890 与标点符号，。！？；：「」（）——以及全角标点的占位对齐。这一段写长一点，以便观察两端对齐后每行的收口：文字文字文字，文字文字，这里有一个批注[^1]，继续把这一行填满以便折行。

### 三级标题

**加粗**、*斜体*、***粗斜体***、~~删除线~~、`行内代码 inline code`、[一个链接](https://example.com)，以及自动链接 https://example.com 的效果。

#### 四级标题

同一段落里也可以有两个批注[^2]，观察它们的堆叠[^3]。

## 列表

无序列表：

- 第一项
- 第二项，写长一点看折行之后与项目符号的对齐效果，文字文字文字文字文字文字文字文字文字文字文字文字
- 嵌套：
  - 子项甲
  - 子项乙
    - 孙项

有序列表：

1. 第一步
2. 第二步
3. 嵌套：
   1. 子步骤一
   2. 子步骤二

任务列表（GFM）：

- [x] 已完成的事
- [ ] 还没做的事
- [ ] 也还没做的事

## 引用

> 单层引用。引用里的**加粗**和 `行内代码`。
>
> 引用的第二段。

> 外层引用。
>
> > 嵌套的内层引用。

## 表格

| 对齐 | 左对齐 | 居中 | 右对齐 |
| ---- | :----- | :--: | -----: |
| 示例 | 文字   | 文字 |   1234 |
| 示例 | 较长的一段文字内容 | 中 | 56.78 |

## 代码块

```js
// JavaScript
const greet = (name) => `你好，${name}`;
console.log(greet('世界'));
```

```python
# Python
def fib(n):
    a, b = 0, 1
    for _ in range(n):
        a, b = b, a + b
    return a
```

```bash
# Shell
npm run build && ls dist/
```

```
无语言标注的纯文本代码块。
```

## 分隔线

分隔线在下面：

---

## 长批注与多段批注

这一段用来测试很长的批注[^4]，正文本身不需要太长。

[^1]: 一条普通长度的批注，说明一下正文里提到的东西。
[^2]: 第一条批注。
[^3]: 第二条批注，和上一条同段，注意两条在右栏里依次堆叠、互不重叠。
[^4]: 这是一条很长的批注，用来测试右栏窄宽度下的折行效果。它有第二段。
    第二段的内容会以换行的形式接在同一条批注里，观察行距和对齐是否正常。
